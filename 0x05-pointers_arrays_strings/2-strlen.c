#include "main.h"
#include <string.h>

/**
 *_strlen - return the length of a string
 *
 *@s: holds the string
 *
 *Return: always 0;
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);

}
